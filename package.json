{
  "name": "lifeline-x-his",
  "version": "1.0.0",
  "description": "Next-generation Hospital Information System with integrated clinical, administrative, and financial workflows",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "seed": "node server/seedData.js",
    "start": "node server/index.js",
    "vercel-build": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.2",
    "pdf-parse": "^1.1.1",
    "sharp": "^0.32.5",
    "socket.io": "^4.7.2",
    "tesseract.js": "^4.1.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}
